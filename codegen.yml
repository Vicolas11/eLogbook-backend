overwrite: true
schema: "src/graphql/schema.ts"
documents: null
generates:
  src/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      scalars:
        DateTime: Date
        URL: string
        JWT: string
