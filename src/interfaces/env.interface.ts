export interface IEnvConfig {
  url: string,  
  port: number;
  dev: boolean;
  prod: boolean;
  test: boolean;
  api_key: string;
  api_secret: string;
  cloud_name: string;
  default_img: string,
}
